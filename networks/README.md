# Network descriptor staging

This directory is intentionally present for production network artifacts.

Official [`networks/mainnet.json`](mainnet.json) and [`networks/testnet.json`](testnet.json) are intentionally absent in this Sprint 1 state.

Prelaunch builds must not invent or ship fake public descriptors, fake chain IDs, or fake launched-network artifacts.
