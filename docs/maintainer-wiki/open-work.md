# Open Work

- Verify whether future repository growth should narrow [`.gitignore`](../../.gitignore) so [`docs/maintainer-wiki/`](./) remains committed while still excluding only local AI state.
- When implementation begins, add evidence-backed entries for actual runtime, dependency manifests, test commands, and validation commands.
- If the full Understand Anything parser-core workflow becomes available in this host, refresh [`.understand-anything/knowledge-graph.json`](../../.understand-anything/knowledge-graph.json) beyond the current minimal documentation-first graph.

